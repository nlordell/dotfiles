# Add local directories to $PATH
export PATH="$HOME/.local/bin:$HOME/.cargo/bin:$PATH"

# Setup some user options
export PAGER="$(which less)"
export EDITOR="$(which nvim)"
export VISUAL="$(which emacs)"
export BROWSER="$(which firefox)"
